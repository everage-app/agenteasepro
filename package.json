{
  "name": "utahcontracts-monorepo",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix web -- --port 5174 --strictPort --host\"",
    "heroku-postbuild": "echo 'Building WEB+SERVER'; cd web && npm ci --include=dev && npm run build && cd ../server && npm ci --include=dev && npx prisma generate && npm run build && cd ..",
    "audit:market-readiness": "node scripts/market_readiness_audit.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
